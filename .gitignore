# Plans & reviews
_plans/
_reviews/
_tmp/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# IDE
*.swp
*.swo
*~
.idea/
.vscode/

# Claude
.claude/
CLAUDE.md

# Go
vendor/
bin/
dist/
coverage.out
coverage.html
*.test
*.prof

# Built binaries, if someone builds in place rather than into bin/
/jr
/jr-agent
/jr-reader
/jr-ci

# Local credentials and state. Nothing under here is ever committed.
*.pem
*.key
.netrc

# Local Data Center recording rig (scripts/dc). The compose env, the licence
# key, and the throwaway jr profile are all local-only.
scripts/dc/.env
scripts/dc/licence.txt
scripts/dc/profile/
