# docs/commands.md is generated by `make docs` from the command registry,
# and internal/lint/docs_test.go compares it byte for byte against what the
# generator produces. A formatter that realigns its tables makes it stale,
# which reads as a stale document rather than as a formatting pass.
docs/commands.md

# Golden files are the output contract. Reformatting one is a contract change
# nobody meant to make.
internal/*/testdata/
internal/*/*/testdata/

# Verbatim, by law and by convention.
LICENSE
NOTICE
