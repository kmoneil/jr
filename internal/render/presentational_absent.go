//go:build !render

package render

// presentationalFormat is the format that is presentation rather than
// contract. This build has none, so nothing matches.
const presentationalFormat Format = ""
