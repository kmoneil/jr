{
  "deployment": "cloud",
  "source": "recorded",
  "note": "Recorded on 2026-08-10 against the Cloud sandbox, unmodified beyond JIRA_RECORD_SCRUB and the response-header filter in transport.KeptHeaders, which drops every header no code reads. `issue edit --parent none` against a company-managed project. The field is sent as an explicit JSON null; an absent field would leave the parent in place, which is the opposite instruction.",
  "interactions": [
    {
      "request": {
        "method": "PUT",
        "path": "/rest/api/3/issue/AGL-3",
        "body": "{\"fields\":{\"parent\":null}}"
      },
      "response": {
        "status": 204
      }
    }
  ]
}
