{
  "deployment": "cloud",
  "source": "recorded",
  "note": "Recorded on 2026-08-10 against the Cloud sandbox, unmodified beyond JIRA_RECORD_SCRUB and the response-header filter in transport.KeptHeaders, which drops every header no code reads. `issue edit --parent` against a company-managed (classic) project. Paired with parentset-nextgen.cloud.json, which is the identical request against a team-managed one: the two together are why the epic verbs do not need to branch on project style, where the agile endpoint they used to call serves company-managed projects only.",
  "interactions": [
    {
      "request": {
        "method": "PUT",
        "path": "/rest/api/3/issue/AGL-3",
        "body": "{\"fields\":{\"parent\":{\"key\":\"AGL-1\"}}}"
      },
      "response": {
        "status": 204
      }
    }
  ]
}
