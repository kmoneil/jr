//go:build tui

// Package tui is the optional interactive terminal UI.
//
// It is a consumer of the resource packages, never a bypass of them, and it is
// absent from any build without the tui tag.
package tui
