{
  "deployment": "cloud",
  "source": "recorded",
  "note": "Recorded on 2026-08-10 against the Cloud sandbox, unmodified beyond JIRA_RECORD_SCRUB and the response-header filter in transport.KeptHeaders, which drops every header no code reads. `epic add` on Cloud, two issues, against a company-managed project. Two requests, because the parent field has no batched spelling \u2014 which is the whole reason this kind gained a per-issue status. It replaces a recording of the agile endpoint, which worked here and is refused on the project style most Cloud callers have; see epicadd-nextgen.cloud.json.",
  "interactions": [
    {
      "request": {
        "method": "PUT",
        "path": "/rest/api/3/issue/AGL-2",
        "body": "{\"fields\":{\"parent\":{\"key\":\"AGL-1\"}}}"
      },
      "response": {
        "status": 204
      }
    },
    {
      "request": {
        "method": "PUT",
        "path": "/rest/api/3/issue/AGL-3",
        "body": "{\"fields\":{\"parent\":{\"key\":\"AGL-1\"}}}"
      },
      "response": {
        "status": 204
      }
    }
  ]
}
